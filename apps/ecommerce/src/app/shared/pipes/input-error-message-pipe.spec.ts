import { InputErrorMessagePipe } from './input-error-message-pipe';

describe('InputErrorMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new InputErrorMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
